// UIManager.cpp carries the module's out-of-line definitions; this translation unit remains so the
// umbrella header is compiled at least once and the target keeps a stable primary source.
#include "UI.h"
